<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="NicoleToolbox.Pages.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NicoleToolbox.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <SelectorBar x:Name="selectorBar" SelectionChanged="SelectorBar_SelectionChanged" Margin="0,0,0,0">
            <SelectorBarItem Text="基础设置" IsSelected="True"/>
            <SelectorBarItem Text="关于"/>
        </SelectorBar>
        <Grid x:Name="grid0" Margin="0,60,0,0">
            <ScrollViewer>
                <Grid VerticalAlignment="Top">
                    <Grid.RowDefinitions>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>
                    <Grid MaxWidth="1024" Grid.Row="0" Padding="25,5,25,5">
                        <InfoBar
                        IsOpen="True"
                        IsClosable="False"
                        MaxWidth="1024"
                        Title="提示"
                        Message="目前设置项无法保存，仅在本次运行生效" >

                        </InfoBar>
                    </Grid>
                    <Grid MaxWidth="1024" Grid.Row="1" Padding="25,5,25,5">
                        <Grid Height="80" Padding="25,10,25,10" CornerRadius="4" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1">
                            <StackPanel Orientation="Horizontal" Spacing="20" VerticalAlignment="Center" HorizontalAlignment="Left">
                                <FontIcon Glyph="&#xE790;"/>
                                <StackPanel Orientation="Vertical">
                                    <TextBlock Text="色彩模式"/>
                                    <TextBlock Text="选择一个色彩模式" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                </StackPanel>
                            </StackPanel>
                            <ComboBox x:Name="ColorModeComboBox" SelectionChanged="ColorModeComboBox_SelectionChanged" PlaceholderText="选择色彩模式" HorizontalAlignment="Right" VerticalAlignment="Center">
                                <ComboBoxItem Content="浅色"/>
                                <ComboBoxItem Content="深色"/>
                                <ComboBoxItem Content="跟随系统"/>
                            </ComboBox>
                        </Grid>
                    </Grid>
                    <Grid MaxWidth="1024" Grid.Row="2" Padding="25,5,25,5">
                        <Grid Height="80" Padding="25,10,25,10" CornerRadius="4" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1">
                            <StackPanel Orientation="Horizontal" Spacing="20" VerticalAlignment="Center" HorizontalAlignment="Left">
                                <FontIcon Glyph="&#xE767;"/>
                                <StackPanel Orientation="Vertical">
                                    <TextBlock Text="启用交互声音"/>
                                    <TextBlock Text="开启后，将开启UI交互声音" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                </StackPanel>
                            </StackPanel>
                            <ToggleSwitch x:Name="SoundToggle" OnContent="开启" OffContent="关闭" Toggled="SoundToggle_Toggled" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                        </Grid>
                    </Grid>
                </Grid>
            </ScrollViewer>
        </Grid>
        <Grid x:Name="grid1" Margin="0,60,0,0">
            <ScrollViewer>
                <Grid>
                    <Border VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0,0,0,250" Padding="8" CornerRadius="8">
                        <StackPanel Orientation="Vertical" Spacing="8">
                            <TextBlock Text="尼可工具箱" FontSize="30" HorizontalAlignment="Center" TextWrapping="Wrap"/>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Spacing="0">
                                <TextBlock Text="版本：" FontSize="20" TextWrapping="Wrap"/>
                                <TextBlock Text="正在获取" FontSize="20" TextWrapping="Wrap" x:Name="VersionTextBlock"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                    <StackPanel Orientation="Vertical" Spacing="8" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,50,0,0">
                        <Button Click="GitHub" Content="GitHub" HorizontalAlignment="Center" Padding="10" Width="100"/>
                        <Button Click="Issue" Content="报告问题" HorizontalAlignment="Center" Padding="10" Width="100"/>
                        <Button Click="Contributors" Content="贡献者" HorizontalAlignment="Center" Padding="10" Width="100"/>
                        <Button Click="Website" Content="官方网站" HorizontalAlignment="Center" Padding="10" Width="100"/>
                    </StackPanel>
                </Grid>
            </ScrollViewer>
        </Grid>
    </Grid>
</Page>
