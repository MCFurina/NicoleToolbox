<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="NicoleToolbox.Pages.Windows.ContributorsWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NicoleToolbox.Pages.Windows"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="ContributorsWindow" x:Name="window">

    <Window.SystemBackdrop>
        <MicaBackdrop/>
    </Window.SystemBackdrop>

    <Grid>
        <TitleBar x:Name="titlebar" Title="贡献者" VerticalAlignment="Top" HorizontalAlignment="Stretch" Height="32"/>
        <ScrollViewer Margin="0,32,0,0">
            <GridView HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,25,0,0" SelectionMode="None">
                <Grid Width="450" Height="150" VerticalAlignment="Center" HorizontalAlignment="Center" CornerRadius="4" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" Padding="10">
                    <Image Source="https://avatars.githubusercontent.com/u/96320440" VerticalAlignment="Center" HorizontalAlignment="Left" Width="64" Height="64"/>
                    <TextBlock Text="MCFurina" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,-30,0,0" FontSize="20"/>
                    <TextBlock Text="本应用作者" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,20,0,0" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    <StackPanel Orientation="Horizontal" Spacing="5" HorizontalAlignment="Right" VerticalAlignment="Bottom">
                        <Button Content="查看GitHub" Click="MCFurina"/>
                        <Button Content="查看网站" Click="Website"/>
                    </StackPanel>
                </Grid>
            </GridView>
        </ScrollViewer>
    </Grid>
</Window>
