<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="NicoleToolbox.Pages.Windows.GIMapWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NicoleToolbox.Pages.Windows"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="GIMapWindow">

    <Window.SystemBackdrop>
        <MicaBackdrop/>
    </Window.SystemBackdrop>

    <Grid>
        <TitleBar x:Name="titleBar" Title="原神地图工具" IsBackButtonVisible="False" IsPaneToggleButtonVisible="False" VerticalAlignment="Top" Height="50">
            <TitleBar.IconSource>
                <ImageIconSource ImageSource="/Assets/Logo.ico"/>
            </TitleBar.IconSource>
            <TitleBar.RightHeader>
                <StackPanel Orientation="Horizontal" Spacing="10">
                    <ToggleSwitch x:Name="pinToTopToggle" OffContent="置顶" OnContent="置顶中" Toggled="PinToTop" IsOn="False"/>
                    <Button Click="Reload">
                        <StackPanel Orientation="Horizontal" Spacing="10">
                            <FontIcon Glyph="&#xE72C;" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                            <TextBlock Text="刷新" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                    <Button Click="OpenWithEdge">
                        <StackPanel Orientation="Horizontal" Spacing="10">
                            <FontIcon Glyph="&#xE8AD;" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                            <TextBlock Text="使用浏览器打开" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </TitleBar.RightHeader>
        </TitleBar>
        <WebView2 x:Name="webview" Source="https://act.mihoyo.com/ys/app/interactive-map/index.html" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="0,50,0,0"/>
    </Grid>
</Window>
